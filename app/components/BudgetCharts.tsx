'use client';
import {ResponsiveContainer,BarChart,Bar,XAxis,YAxis,CartesianGrid,Tooltip,Legend,LineChart,Line} from 'recharts';
import type {analytics} from '@/lib/journal';
export default function BudgetCharts({data}:{data:ReturnType<typeof analytics>}){return <div className="mt-6 grid gap-5 lg:grid-cols-2"><section className="panel min-w-0"><h2 className="text-xl font-bold">Planned vs recorded spending</h2><p className="mt-2 text-xs muted">INR for the whole group. The reserve is kept separate from spending.</p><div className="mt-5 h-80 w-full"><ResponsiveContainer width="100%" height="100%"><BarChart data={data.categories} margin={{left:0,right:8,bottom:20}}><CartesianGrid strokeDasharray="3 3" vertical={false}/><XAxis dataKey="name" tick={{fontSize:10}} interval={0} angle={-20} textAnchor="end"/><YAxis tick={{fontSize:11}}/><Tooltip/><Legend verticalAlign="top"/><Bar name="Planned" dataKey="planned" fill="#bccfe5" radius={[5,5,0,0]}/><Bar name="Recorded" dataKey="actual" fill="#075b3d" radius={[5,5,0,0]}/></BarChart></ResponsiveContainer></div></section><section className="panel min-w-0"><h2 className="text-xl font-bold">How spending added up</h2><p className="mt-2 text-xs muted">Cumulative recorded expenses by payment date.</p>{data.daily.length?<div className="mt-5 h-80 w-full"><ResponsiveContainer width="100%" height="100%"><LineChart data={data.daily} margin={{left:0,right:15,bottom:15}}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="date" tick={{fontSize:10}}/><YAxis tick={{fontSize:11}}/><Tooltip/><Line type="monotone" dataKey="cumulative" name="Cumulative INR" stroke="#075b3d" strokeWidth={3}/></LineChart></ResponsiveContainer></div>:<p className="mt-14 text-sm muted">Add an expense to see the trend. A zero total means no expenses have been recorded yet.</p>}</section></div>;}
